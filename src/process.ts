const handleVerificationProcess = async () => {
  try {
    await processVerificationRequest(); // Проверка запроса
    await makeUpdateRequest();    // Обновление данных
    await processDeletion(); // Удаление старых записей
  } catch (error) {
    console.error('Error during the verification process:', error);
    // Логика обработки ошибки или отката
  }
};

// Эти три функции получается должны быть асинхронными

/*Что было плохо:
1) Возможно, тут работа с асинхронным кодом
Неясно, в каком порядке и почему вызываются эти функции
Например, если одна из функций не выполнится успешно, может потребоваться обработка ошибки или отмена выполненных операций
*/

/*Что стало лучше:
1) Явное указание порядка выполнения и зависимостей, обработка ошибок
 Логика выполнения функций была объединена в одну функцию, которая управляет последовательностью шагов
*/